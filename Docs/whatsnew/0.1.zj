# zunjin 0.2.4 更新日志

## IDE 补全弹窗重构（彻底解决"弹窗干扰打字/点击"）

- **补全框永不抢焦点**（WS_EX_NOACTIVATE）：焦点始终留在编辑器，
  打字、Shift 组合键（如 `{`）、点击其他区域都不受弹窗干扰。
- **键盘由编辑器统一处理**：补全打开时 ↑/↓ 导航、Enter/Tab 插入、
  Esc 关闭在编辑器的消息过滤器里拦截，其余按键照常输入。
- **自动补全降噪**：要求前缀 ≥2 字符才自动弹框（单打一个字母不再弹），
  弹窗位置未变化时不重复移动（消除逐键抖动）；语法高亮防抖 150→250ms。
- 修复补全框抢占焦点导致 Shift+{ 等组合键失灵、点击其他区域无响应的问题。

---

# zunjin 0.2.3 更新日志

## 编辑器交互修复（打字闪烁 / 自动补全 / 提示框）

- **修复打字时视图"往上翻又跳回"的闪烁**：语法高亮批量着色时每个
  EM_SETSEL 都会让 RichEdit 滚动视图。改用 WM_SETREDRAW 屏蔽批量着色期间
  的重绘，整体重置改用 SCF_ALL（不动选区），最后稳妥恢复滚动与光标——
  高亮刷新完全不可见。
- **新增 IntelliJ 式自动补全**：输入标识符字符（字母/下划线/数字）防抖
  120ms 自动弹出补全提示框，继续打字实时过滤、空格/标点自动关闭，
  单击即插入；补全插入后不再重复弹出。
- **修复补全弹窗被高亮格式化误杀**：高亮批量着色产生的 EN_CHANGE 会
  误触发补全刷新并销毁弹窗，现已跳过（g_hiliteBusy 期间不刷新补全）。
- **修复弹窗位置**：光标靠近窗口底部时弹窗自动翻转到上方（IntelliJ 行为），
  并限制在主窗口可见范围内。

---

# zunjin 0.2.2 更新日志

## IDE 交互修复（弹窗"点了没反应"与覆盖编辑器）

- **补全弹窗单击即插入**：原来单击只高亮、必须双击才生效（IntelliJ 是单击选中即插入）。
- **搜索/结构/最近弹窗单击即跳转**：同时修复点击命中公式在非搜索模式下
  全部失效的问题（列表起始 Y 偏移写死为搜索模式的 66，结构/最近模式从 30 开始）。
- **点击编辑器 / 切换到其他窗口时弹窗自动关闭**：补全与搜索弹窗不再
  一直盖在编辑器上方关不掉（IntelliJ 行为）。

## IDE 渲染修复

- **DPI 感知提前**：Per-Monitor V2 声明移到 WinMain 最开头（原来在创建启动画面
  之后才调用，导致 120% 高分屏下进程被虚拟化拉伸：界面模糊、弹窗坐标错位）。
- **画刷缓存加固**：容量从 24 扩到 256 并支持淘汰最旧；此前欢迎页渐变（50+ 色）
  会塞满缓存导致后续填充退化为空心刷、背景露出白色。

---

# zunjin 0.2.1 更新日志

## 性能优化（第二轮）

- **环境函数链哈希化**：全局环境约 80 个内置函数，每次函数/内置调用
  不再线性扫描函数链（哈希索引 O(1)）。函数密集负载提速约 26%。
- **词法器关键字二分查找**：关键字表按字典序排列 + 二分查找（O(log n)）。
- **Token 空闲链表**：解析期 token 结构复用，减少大型源码的 malloc/free。

基准（函数密集负载）：1064ms → 789ms（约 26%）；综合负载约 52% 于 0.1.0。

## IDE 优化（第二轮，IntelliJ 思路）

- **结构视图符号缓存**：文档未变化时复用符号表（长度+哈希校验），
  结构面板/补全/跳转定义不再重复全量扫描。
- **全局搜索文件列表索引**：首次搜索枚举文件并记录目录时间戳，
  后续搜索校验时间戳后直接复用文件列表，跳过整树重枚举。

---

# zunjin 0.2.0 更新日志

## 性能优化（解释器提速约 50%）

- **枚举操作码分发**：运算符从字符串比较改为枚举（`ZjnOp`），
  消除热路径中的 strcmp 开销（等价字节码操作码设计思路）。
- **有序哈希字典**：字典改为"有序数组 + 哈希索引"（负载因子 2/3 扩容），
  查找由 O(n) 降为 O(1)，同时保持插入顺序（遍历/打印/foreach 稳定）。
- **哈希环境 + 参数所有权转移**：作用域变量查找哈希化；
  函数调用参数直接接管所有权，省去一次引用计数 copy/free。
- **值空闲链表**：引用计数归零的值结构进入空闲链表复用，
  大幅减少算术热循环中的 malloc/free。

## 新特性：模块系统

- `use` 语句加载模块（模块缓存等价 sys.modules 思路），支持
  `模块名.成员` 属性访问，模块函数自动获得模块命名空间闭包。
- 标准库 StdLib/ 全部用 zunjin 语法重写（math/string/types/
  collections/functools/itertools/random/json/re/logging/pathlib 等）。
- 修复词法器多级 DEDENT 丢失的潜在缺陷（一次回退多层缩进）。

## 其余

- `delete` / `clear` 内置函数现支持字典（按键删除 / 清空）。

---

# zunjin 0.1.0 更新日志
# 初始版本特性：
# - 词法分析器 (Lexer)
# - 语法解析器 (Parser) 
# - AST 节点管理
# - 虚拟机执行器 (VM)
# - 环境与作用域管理
# - 对象系统
# - 内置函数
# - 控制台输出模块
