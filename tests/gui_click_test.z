# ============================================================
# zunjin GUI 点击测试：验证 wait_click 事件循环
# 由外部工具模拟按钮点击后自动结束
# ============================================================

win = window('点击测试', 260, 150)
label(win, '请点击按钮', 20, 20)
btn = button(win, '点我', 20, 50, 80, 30)

show(win)
put('等待点击...')

cid = wait_click(win)
put('收到点击，控件 id: ' + text(cid))
when cid == btn:
    put('按钮匹配成功')
otherwise:
    put('FAIL: 控件 id 不匹配')

close(win)
sleep(200)
put('测试完成')
